#include "main.h"
int kmain(void){
	main();
	return;
}
/*
#include "screen.h"
#include "idt.h"

void kmain(void);
int putInt(int);
void kmain(void){
	Point point;
	point.x = point.y = 0;
	clearScreen(COLOR_WHITE);
	drawString("0123456789\r\nabcdefg\r\nhijklnm\r\nopqrstuv\r\nwxyz", point, COLOR_BLACK);

	
	
	if(sizeof(TaskGateDescriptor) == 64){
		drawString("a", point, COLOR_BLACK);
	}
	
	int size = sizeof(TaskGateDescriptor);
	putInt(size);
	return;
	
}

void stringRightShift(char *value, int count){
	int i;
	for(i = count - 2; i > 0; i--){
		value[i + 1] = value[i];
	}
	value[0] = ' ';
}
int putInt(int a){
	char aa[100];
	int i = 0;
	for(i = 0; i < 100; i++){
		aa[i] = '\0';
	}
	while(1){
		int digit = a % 10;
		aa[0] = digit + '0';
		a /= 10;
		if(a == 0){
			break;
		}else{
			stringRightShift(aa, 100);
		}
	}
	
	Point point;
	point.x = point.y = 0;
	drawString(aa, point, COLOR_BLACK);
}
*/